export const resizeTextarea = () => {
    // const textarea = document.getElementsByTagName('description')[0];

    // textarea.addEventListener('keyup', function(event){
    //     if(docu.scrollTop > 0){
    //         this.style.height = this.scrollHeight + "px";
    //     }
    // });
}